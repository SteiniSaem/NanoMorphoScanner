main() {
	var a;
	var b,c,d;

	a = 3;
	b = 7;
	c = 10;
	d = 13;

	if (a < b || c > 0 && d^(a-b*c) <= a && a != b || false) {
		return a+b;
	} elsif (a > b) {
		a*b;
	};

	while (a < b && a == b) {
		a = a+1;
	};

	if (a != b) {
		a = a&b;
	} else {
		+a-b;
		a- +b;
		# Single line comment
	};
	###
	var varg = "returnaddress";
	var var = return;
	return ++b - (b%a);
	###

}
